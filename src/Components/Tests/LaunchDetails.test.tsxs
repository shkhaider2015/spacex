import { shallow, ShallowWrapper } from "enzyme";
import { LaunchDetails } from "../LaunchDetails";


describe("", () => {
  let wrapper: ShallowWrapper;
  beforeEach(() => {
    wrapper = shallow(<LaunchDetails />)
  })

  test('LaunchDetails : Test App Component', () => {
    // expect(wrapper.find(".container").text()).toBe(`<ApolloProvider />`);
    wrapper.debug();
  });

})